## Bug Fixes
- Fixed settings not loading correctly for new settings added in future updates
- Fixed "Open and search" command failing due to missing method
- Fixed deleting an omitted file incorrectly removing the last file from the list
- Fixed search highlighting producing incorrect results for accented or mixed-case text
- Fixed arrow key navigation crashing when no file is active
- Fixed Svelte component not being cleaned up when sidebar view closes
- Fixed sidebar view not being revealed when in a collapsed sidebar
- Fixed potential crash when accessing the list view before it is fully loaded
- Fixed unbounded recursion when creating a new note fails repeatedly
