## Features
- Added multi-select support — Shift+Click to select a range, Alt+Click to toggle individual files, with a bulk context menu (pin/unpin, move to trash) for multi-selected files
- The refresh action is now always visible in the action menu
